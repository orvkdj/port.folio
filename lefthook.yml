pre-commit:
  parallel: true
  jobs:
    - name: lint-staged
      run: pnpm lint-staged

commit-msg:
  parallel: true
  jobs:
    - name: commitlint
      run: pnpm commitlint --edit {1}

    - name: spelling
      run: pnpm check:spelling {1}
